{
  "name": "gmail-to-drive",
  "version": "1.0.0",
  "description": "Chrome extension to save Gmail emails as PDFs with attachments to Google Drive",
  "private": true,
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "package": "npm run build && node scripts/package.js"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "webpack": "^5.97.0",
    "webpack-cli": "^6.0.0"
  },
  "dependencies": {
    "html2pdf.js": "^0.10.2"
  }
}
